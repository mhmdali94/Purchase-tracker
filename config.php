<?php
/**
 * config.php
 * ------------------------------------------------------------------
 * إعدادات الاتصال بقاعدة البيانات + إعدادات عامة للتطبيق.
 * القيم الافتراضية هنا مضبوطة على إعدادات XAMPP الافتراضية:
 *   - الخادم: localhost
 *   - المستخدم: root
 *   - كلمة المرور: (فارغة)
 * عدّل هذه القيم فقط إذا كانت إعدادات MySQL لديك مختلفة.
 * ------------------------------------------------------------------
 */

// ---- بيانات الاتصال بقاعدة البيانات ----
define('DB_HOST', 'localhost');
define('DB_NAME', 'purchase_tracker');
define('DB_USER', 'root');
define('DB_PASS', '');          // كلمة مرور فارغة (الوضع الافتراضي في XAMPP)
define('DB_CHARSET', 'utf8mb4');

// ---- إعدادات عامة ----
define('APP_NAME', 'متتبّع أسعار المشتريات');
define('CURRENCY', 'ج.م');       // الجنيه المصري

// ضبط المنطقة الزمنية لعرض التواريخ بشكل صحيح
date_default_timezone_set('Africa/Cairo');

// عرض الأخطاء أثناء التطوير — يمكن تعطيلها في الإنتاج
error_reporting(E_ALL);
ini_set('display_errors', '1');
